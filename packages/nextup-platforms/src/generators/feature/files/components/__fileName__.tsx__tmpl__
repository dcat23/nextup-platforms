"use client";

import React, { ReactNode } from 'react';

interface Props {
  data?: any;
  children?: ReactNode;
}

export function <%= className %> ({ data, children }: Props) {
  return (
    <>
      <%= className %>
      {children}
    </>
  );
};
